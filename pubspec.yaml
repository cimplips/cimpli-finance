name: keuangan_prima
description: Production-ready personal finance starter.
publish_to: "none"
version: 1.1.0+2

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  sqflite: ^2.3.3+1
  path: ^1.9.0
  shared_preferences: ^2.3.2
  uuid: ^4.5.1
  local_auth: ^2.3.0
  file_picker: 8.0.7
  flutter_plugin_android_lifecycle: 2.0.19

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
